{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "prestart": "npm run build",
    "start": "node .",
    "dev": "nodemon --exec babel-node ./src/index.js --presets=@babel/preset-env",
    "test": "jest",
    "clean": "rimraf dist",
    "transpile": "babel ./src --out-dir=dist --presets=@babel/preset-env --source-maps",
    "build": "npm run clean && npm run transpile"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.11.6",
    "@babel/polyfill": "^7.11.5",
    "@babel/preset-env": "^7.11.5",
    "jest": "^26.4.2",
    "rimraf": "^3.0.2",
    "supertest": "^4.0.2"
  },
    "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "graphql": "^15.3.0",
    "mongoose": "^5.10.4",
    "morgan": "^1.10.0"
  },
  "jestConfig": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "node_modules",
      "dist"
    ]
  },
  "babelrc": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
